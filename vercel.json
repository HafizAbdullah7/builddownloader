{
  "version": 2,
  "builds": [
    { "src": "build/**", "use": "@vercel/static" },
    { "src": "index.js", "use": "@vercel/node" }
    
  ],
  "routes": [
    { "src": "/build/(.*)", "dest": "/build/$1" },

    { "src": "/(.*)", "dest": "/index.js" }
  ]
}
